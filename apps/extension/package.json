{
  "name": "@resumeseeker/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html popup.css dist/",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
